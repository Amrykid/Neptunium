<Page
    x:Class="Neptunium.View.Dialog.ProgressIndicatorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Neptunium.View.Dialog"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Transparent" Height="Auto">

        <Grid HorizontalAlignment="Center" Padding="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="PART_TitleBlock" Text="Title" Grid.Row="0" VerticalAlignment="Bottom"
                       Style="{ThemeResource BaseTextBlockStyle}" />
            <TextBlock x:Name="PART_MessageBlock" Text="Message" Grid.Row="1"
                       Style="{ThemeResource BodyTextBlockStyle}" />

            <ProgressBar x:Name="PART_ProgressIndicator" Grid.Row="2" Height="12" />
        </Grid>
    </Grid>
</Page>
